{
  "name": "Refractive Crystal (Black)",
  "code": "#define DTR 0.01745329\n#define rot(a) mat2(cos(a),sin(a),-sin(a),cos(a))\n\nvec2 uv;\nvec3 cp,cn,cr,ro,rd,ss,oc,cc,g_val,vb;\nvec4 fc;\nfloat tt,cd,sd,io,oa,td;\nint es=0, ec=0;\n\nfloat bx(vec3 p,vec3 s){vec3 q=abs(p)-s;return min(max(q.x,max(q.y,q.z)),0.)+length(max(q,0.));}\nfloat cy(vec3 p, vec2 s){p.y+=s.x/2.;p.y-=clamp(p.y,0.,s.x);return length(p)-s.y;}\n\nfloat shatter(vec3 p, float d, float n, float a, float s)\n{\n    for(int i=0; i<4; i++)\n    {\n        if(float(i) >= n) break;\n        p.xy*=rot(a);p.xz*=rot(a*0.5);p.yz*=rot(a+a);\n        float c=mod(float(i),3.)==0.?p.x:mod(float(i),3.)==1.?p.y:p.z;\n        c=abs(c)-s;d=max(d,-c);\n    }\n    return d; \n}\n\nvec3 lattice(vec3 p, int iter)\n{\n        for(int i = 0; i < 8; i++)\n        {\n            if(i >= iter) break;\n            p.xy *= rot(45.*DTR);\n            p.xz *= rot(45.*DTR);\n            p=abs(p)-1.;\n            \n            p.xy *= rot(-45.*DTR);\n            p.xz *= rot(-45.*DTR);\n        }\n        return p;\n}\n\nfloat mp(vec3 p)\n{\n    if(iMouse.z > 0.){\n        p.yz *= rot(2.0 * (iMouse.y / iResolution.y - 0.5));\n        p.zx *= rot(-7.0 * (iMouse.x / iResolution.x - 0.5));\n    }\n    vec3 pp = p;\n    \n    p.xz *= rot(tt * 0.2);\n    p.xy *= rot(tt * 0.2);\n\n    p = lattice(p, 3);\n\n    sd = cy(p, vec2(1.)) - 0.05;\n\n    sd = shatter(p, sd, 1., sin(tt * 0.1), 0.2);\n\n    sd = min(sd, bx(p, vec3(0.1, 2.1, 8.)) - 0.3);\n\n    sd = mix(sd, cy(p, vec2(4, 1)), cos(tt * 0.5) * 0.5 + 0.5);\n\n    sd = abs(sd) - 0.001;\n    if(sd < 0.001)\n    {\n        oc = mix(vec3(0.0, 0.0, 0.0), vec3(0., 0.6, 1.), pow(length(pp) * 0.18, 1.5));\n        io = 1.1;\n        oa = 0.05 + 1. - length(pp) * 0.2;\n        ss = vec3(0.);\n        vb = vec3(0., 2.5, 2.5);\n        ec = 2;\t\n    }\n    return sd;\n}\n\nvoid tr(){\n    vb.x = 0.; cd = 0.; \n    for(int i=0; i<512; i++){\n        mp(ro + rd * cd);\n        cd += sd; td += sd;\n        if(sd < 0.0001 || cd > 128.) break;\n    }\n}\n\nvoid nm(){\n    mat3 k = mat3(cp, cp, cp) - mat3(.001);\n    cn = normalize(mp(cp) - vec3(mp(k[0]), mp(k[1]), mp(k[2])));\n}\n\nvoid px()\n{\n    cc = vec3(0.0, 0.0, 0.0) + length(pow(abs(rd + vec3(0, 0.5, 0)), vec3(3.0))) * 0.3 + g_val;\n    if(cd > 128.){ oa = 1.; return; }\n    vec3 l = vec3(0.4, 0.7, 0.8);\n    float df = clamp(length(cn * l), 0., 1.0);\n    vec3 fr = pow(1.0 - df, 3.0) * mix(cc, vec3(0.4), 0.5);\n    float sp = (1.0 - length(cross(cr, cn * l))) * 0.2;\n    float ao = min(mp(cp + cn * 0.3) - 0.3, 0.3) * 0.5;\n    cc = mix((oc * (df + fr + ss) + fr + sp + ao + g_val), oc, vb.x);\n}\n\nvoid render(vec2 frag, vec2 res, float time, out vec4 col)\n{\n    tt = mod(time, 260.0);\n    uv = vec2(frag.x / res.x, frag.y / res.y);\n    uv -= 0.5; uv /= vec2(res.y / res.x, 1.0);\n    ro = vec3(0, 0, -15); rd = normalize(vec3(uv, 1));\n    \n    fc = vec4(0.0);\n    for(int i=0; i<20; i++)\n    {\n        tr(); cp = ro + rd * cd;\n        nm(); ro = cp - cn * 0.01;\n        cr = refract(rd, cn, (i % 2 == 0) ? 1.0 / io : io);\n        if(length(cr) == 0.0 && es <= 0){ cr = reflect(rd, cn); es = ec; }\n        if(es > 0 && (es % 3 == 0) && cd < 128.0) rd = cr; es--;\n        if(vb.x > 0.0 && (i % 2 == 1)) oa = pow(clamp(cd / vb.y, 0.0, 1.0), vb.z);\n        px(); fc = fc + vec4(cc * oa, oa) * (1.0 - fc.a);\t\n        if((fc.a >= 1.0 || cd > 128.0)) break;\n    }\n    col = fc / fc.a;\n}\n\nvoid mainImage(out vec4 fragColor, in vec2 fragCoord) {\n    render(fragCoord, iResolution.xy, iTime, fragColor);\n}"
}
